// Project and spec links — import and use via `#link-name`
//   #import "../links.typ"
//   See #links.spec-cli for details.

// ── Spec references ──────────────────────────

#let spec-cli = link("specs/cli.typ")[cli.typ]
#let spec-config = link("specs/config.typ")[config.typ]
#let spec-module-api = link("specs/module-api.typ")[module-api.typ]
#let spec-ai-skills = link("specs/ai-skills.typ")[ai-skills.typ]
#let spec-docs = link("specs/docs.typ")[docs.typ]
#let spec-publishing = link("specs/publishing.typ")[publishing.typ]
#let spec-archive = link("specs/archive.typ")[archive.typ]
#let spec-scaffold = link("specs/scaffold.typ")[scaffold.typ]
#let spec-install = link("specs/install.typ")[install.typ]

// ── Project files ────────────────────────────

#let glossary = link("glossary.typ")[glossary.typ]

// ── Typst documentation ──────────────────────

#let typst-bibliography = link("https://typst.app/docs/reference/model/bibliography/")[bibliography]
#let typst-module = link("https://typst.app/docs/reference/foundations/module/")[module]

// ── Typst packages ───────────────────────────

#let pkg-glossarium = link("https://typst.app/universe/package/glossarium")[glossarium]
