[package]
name = "typspec-core"
version = "0.1.0"
edition = "2024"
description = "Core library for typspec — AST surgery, metadata querying, config parsing"

[dependencies]
typst-syntax = "0.14"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
