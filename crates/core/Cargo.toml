[package]
name = "typspec-core"
version = "0.1.0"
edition = "2021"
description = "Core library for typspec — config parsing, metadata querying, AST surgery"

[dependencies]
typst-syntax = "0.14"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
