[package]
name = "typspec"
version = "0.1.0"
edition = "2021"
description = "CLI for typspec — specification management powered by Typst"

[dependencies]
typspec-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
